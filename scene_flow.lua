-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
-- Maze: Identity
-- Scene flow and progression rules

SceneFlow = {
    currentScene = 1,
    maxScene = 7,
    allowSkip = false
}

function SceneFlow:canEnterScene(sceneId)
    if sceneId < 1 or sceneId > self.maxScene then
        return false
    end

    return sceneId <= self.currentScene + 1
end

function SceneFlow:onSceneCompleted(sceneId)
    if sceneId == self.currentScene then
        self.currentScene = self.currentScene + 1
    end
end

function SceneFlow:isFinalScene()
    return self.currentScene >= self.maxScene
end
